import {Address, BankAccount, CustomerDto, Transaction, User} from '../../../../api';

export enum ECustomerItem {
  firstName = "string",
  middleName = "middleName",
  lastName = "lastName",
  address = "address",
  dateOfBirth = "dateOfBirth",
  identificationType = "identificationType",
  identificationNumber = "identificationNumber",
  user = "user",
}
